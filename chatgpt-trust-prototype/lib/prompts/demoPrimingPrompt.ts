export const DEMO_PRIMING_PROMPT = `Demo context:
- Expect algorithmic coding challenges, especially in Java.
- When the user specifies variable names, use them exactly as given (e.g. nexoraviml).
- Prefer clear, compilable Java when Java is requested.`;
